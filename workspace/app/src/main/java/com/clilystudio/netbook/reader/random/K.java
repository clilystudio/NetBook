package com.clilystudio.netbook.reader.random;

import com.clilystudio.netbook.reader.cd;
import com.clilystudio.netbook.reader.o;

final class K
        implements cd {
    K(ReaderRandomActivity paramReaderRandomActivity) {
    }

    public final void a() {
        o[] arrayOfo = ReaderRandomActivity.i(this.a);
        int i = arrayOfo.length;
        for (int j = 0; j < i; j++)
            arrayOfo[j].c();
    }
}

