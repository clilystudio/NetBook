package com.clilystudio.netbook.widget;

public enum ThemeLoadingView$Mode {
    static {
        DARK = new Mode("DARK", 1);
        Mode[] arrayOfMode = new Mode[2];
        arrayOfMode[0] = LIGHT;
        arrayOfMode[1] = DARK;
        a = arrayOfMode;
    }
}

